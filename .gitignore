# Virtual environment & credentials
.venv/
venv/
.env
.tmp/
.pytest_cache/
__pycache__/
*.pyc
*.pyo
*.pyd

# Databases
db/
*.db
*.sqlite
*.sqlite3
*.duckdb

# ── dirtywork/ ────────────────────────────────────────────────────────────────
# Contains: raw JSONL profile cards, binary index artifacts, one-off build
# scripts, scrapers, raw datasets, archives, and exploration notebooks.
# Nothing in dirtywork/ belongs in source control.
dirtywork/

# Large binary / generated index artifacts (if ever outside dirtywork/)
*.hnsw
*.pkl
*.npy
*.gguf
*.bin
*.pt
*.safetensors

# Large data dumps
*.jsonl
*.csv
*.zip
*.tar.gz
*.bak

# Misc tooling
.DS_Store
*.log

# Eval runtime artifacts
eval/qul_cache.json
